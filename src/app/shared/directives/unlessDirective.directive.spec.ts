/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UnlessDirectiveDirective } from './unlessDirective.directive';

describe('Directive: UnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlessDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
